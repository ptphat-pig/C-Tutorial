# Weekly Report
## HW-150925

### Part: [part name]

#### Exercises <number>
+ What?
+ How to do?
+ Problems?
+ Solution? - Supervisor
### Part: Basic
#### Example 1:
+ Đề bài: Sử dung C++ để viết chương trình in ra văn bản chào mừng trên dòng đặc biệt?
+ Cách phân tích:
. xuất dữ liệu ra màn hình 
. dùng cout để in kết quả ra màn hình 
. chèn thêm "\n" hoặc endl để xuống dòng 
#### Example 2:
+ Đề bài: Viết chương trình C++ để in ra tổng của 2 số.
+ Cách phân tích:
. Dùng int để khai báo hai biến a và b để nhập vào hai số cần tính tổng.
. Dùng int để khai báo biến sum là tổng hai số a và b.
. gán a = 29, b = 30.
. Dung cout để in ra màn hình tổng của hai số a và b( sum = a + b).
+ Vấn đề: 
. Hay comment dư thừa trong code
. Đọc nhầm đề

#### Example 3:
+ Đề bài: Viết chương trình C++ để tìm kích thước của các kiểu dữ liệu cơ bản.
+ Cách phân tích:
. Dùng cout để in ra màn hình kích thước của các kiểu dữ liệu như char, short, int, long, long long, float, double, long double, bool.
. Dùng toán tử sizeof() để lấy kích thước của kiểu dữ liệu
+ Vấn đề:
. Chưa nắm được các kiểu dữ liệu.
. Chưa thuộc được các kiểu dữ liệu sẽ có kích thước bằng bao nhiêu.
. Chưa nắm được các phép toán tử.

#### Example 4:
+ Đề bài: Viết chương trình C++ để ra tổng của hai số bằng cách sử dụng biến.
+ Cách phân tích:
. Dùng int để khai báo hai biến num1 và num2.
. Dùng int để khai báo biến Tong = num1 + num2.
. Gán giá trị num1 = 29, num2 = 30.
. Dùng cout để in ra kết quả của biến Tong

#### Example 5:
+ Đề bài: Viết chương trình C++ để kiểm tra giới hạn trên và dưới của các kiểu dữ liệu số nguyên.
+ Cách phân tích: 
. Dùng cout để in ra chủ đề của bài tập là kiểm tra giới hạn trên và dưới của các kiểu dữ liệu
. Dùng cout để in ra giá trị maximum và minimum của các kiểu dữ liệu dựa trên hằng số( constaint macro) được định nghĩa trong thư viện <climits> như INT8_MAX/MIN, INT16_MAX/MIN, INT32_MAX/MIN, INT64_MAX/MIN,..
+ Vấn đề:
. Chưa nắm vững giới hạn trên và dưới của các kiểu dữ liệu nên sử dụng hằng số nào trong <climits>

#### Example 6:
+ Để bài: Viết chương trình C++ để xem các giá trị nguyên bản có vượt quá giới hạn hay không
+ Cách phân tích: 
. Sử dụng biến char để khai báo theGender = 'F'
. Sử dụng biến int để khai báo herMarried = 1, herSons = 2, herApointment = 2009, herSalary = 1500000, herSalaryDrawUpto = 12047235, herBalanceTill = 995324987.
. Sử dụng biến double để khai báo herHeight = 79.48, herGPA = 4.69
. Sử dụng cout để in ra các giá trị nguyên bản của các biến đã khai báo
+ Vấn đề:
. Chưa nắm vững được đề bài, dịch ẩu.
. Chưa nắm vững được tất cả các kiểu biến.

#### Example 7:
+ Đề bài: Viết chương trình C++ hiển thị các kiểu dữ liệu hỗn hợp và các phép toán số học.
+ Cách phân tích:
. Dùng int để khai báo hai biến m1 = 5 và m2 = 7
. Dùng double để khai báo hai biến m3 = 3.7 và m4 = 8.0
. Dùng cout để in ra kết quả của các phép toán m1 + m2; m3 + m4; m1 + m4; m1 - m2; m3 - m4; m1 - m4;
m1 * m2; m3 * m4; m1 * m4; m1 / m2; m3 / m4; m1 / m4
+ Vấn đề: 
. không có

#### Example 8:
+ Đề bài: Viết chương trình C++ để kiểm tra tràn (overflow) / hụt (underflow) trong quá trình thực hiện các phép toán số học khác nhau.
+ Cách phân tích:
. Dùng int để khai báo n1 = -2147483648, n2 = 2147483648.
. Dùng cout để in ra giá trị của các phép toán n1, n1 + 1, n1 * n1; n2, n2 - 1, n2 * n2;
+ Vấn đề:
. không có

#### Example 9:
+ Đề bài: Viết chương trình C++ để hiển thị cách hoạt động của toán tử tăng/giảm trước (pre-increment/decrement) và sau (post-increment/decrement).
+ Cách phân tích:
. Dùng int để khai báo num = 57.
. Dùng các phép toán tử để tăng hoặc giảm 1 đơn vị như ++num, num++, --num, num--.
. Dùng cout để in ra các giá trị sau khi đã tăng 1 đơn vị hoặc giảm đi 1 đơn vị.
+ Vấn đề: 
. Không có

#### Example 10:
+ Đề bài: Viết chương trình C++ để định dạng đầu ra.
+ Cách phân tích: 
. Sử dụng biến double để khai báo pi = 3.1416
. Sử dụng biến setw() để mở rộng trường hiển thi
. Dùng cout để in ra giá trị từng dòng theo dạng scientific notation.
+ Vấn đề :
. Các kiểu biến mới không cs trong phần tutorial nên phải tra cứu trên các trang web để tìm hiêu

#### Example 11:
+ Đề bài: Viết chương trình C++ để in kết quả của các phép toán đã chỉ định.
+ Cách phân tích: 
. Sử dụng cout để in ra màn hình các phép tính (6 * 4 - 1), (30 + 5)/7, (14+-4*6/11), (2+15/6*1-7%2).
+ Vấn đề: 
. Không có.

#### Example 12: 
+ Đề bài: Viết chương trình C++ để cộng hai số và nhập chúng từ bàn phím.
+ Cách phân tích:
. Sử dụng biến int để khai báo num0 = 25, num10 = 39; sump = num0 + num10.z
. Sử dụng cout để in ra giá trị của num0 = 25, num10 = 39, sump = 64z
+ Vấn đề:
. KHông có

#### Example 13:
+ Đề bài: iết chương trình C++ để hoán đổi (swap) giá trị của hai số.
+ Cách phân tich:
. Cho người dùng nhập vào hai biến u và i là số nguyên
. Sử dụng các phép tính u = u + i
                        i = u - i
                        u = u - i 
. Dùng cout để in ra kết quả swau khi đã thay đổi vị trí của hai biến u và iz
+ Vấn đề:
. Chưa hình dung rõ đc cách hoạt động của phép toán để tha y đổi vị trí

#### Example 14:
+ Đề bài: Viết chương trình C++ để tính thể tích của một hình cầu khi biết bán kính.
+ Cách phân tích: 
. Dùng biến double để khai báo bán kính R = 6 
                                        volumeSphere = 4.0 / 3.0 * M_PI * pow(R, 3)
. Dùng cout để in ra bán kính R = 6 và kết qủa của volumeSphere 
+ Vấn đề:
. Chưa thuộc công thức toán

#### Example 15:
+ Đề bài: Viết chương trình C++ để tính thể tích của một hình lập phương (cube)
+ Cách phân tích:
. Dùng biến double để khai báo sideCube = 5 và volumeCube = pow(sideCube, 3)z
. Dùng cout để in ra sideCube = 5 và kết quả của volumeCube
+ Vấn đề:
. Chưa thuộc công thức toán

#### Example 16:
+ Đề bài: Viết chương trình C++ để tính thể tích của một hình trụ (cylinder).
+ Cách phân tích:
. Dùng biến double để khai báo radiusCylinder = 6
                               heightCylinder = 8 
                               volumeCylinder = pi * pow(radiusCylinder,2) * heightCylinder
. Dùng cout để in ra radiusCylinder = 6, heightCylinder = 8 và kết qủa của volumeCylinder
+ Vấn đề: 
. Chưa thuộc công thức toán  

#### Example 17:  
+ Đề bài: Viết chương trình C++ để tìm diện tích và chu vi của hình chữ nhật.
+ Cách phân tích:
. Sử dụng biến double để khai báo lengthRectangle = 10;
                                  widthRectangle = 15;
                                  areaRectangle = lengthRectangle * widthRectangle;
                                  perimeterRectangle = (lengthRectangle + widthRectangle) * 2;
. Dùng cout để in ra màn hình kết quả của lengthRectangle, widthRectangle, areaRectangle, perimeterRectangle
+ Vấn đề: 
. Không nhớ công thức toán

#### Example 18: 
+ Đề bài: Viết chương trình C++ để tìm diện tích của một tam giác bất kỳ sử dụng công thức Heron.
+ Cách phân tích:
. Dùng double để khai báo:
    double lengthOf1stTriangle = 5
    double lengthOf2ndTriangle = 5 
    double lengthOf3rdTriangle = 5
    double areaTriangle = (lengthOf1stTriangle + lengthOf2ndTriangle + lengthOf3rdTriangle) / 2
. Dùng cout để in ra giá trị của lengthOf1stTriangle = 5, lengthOf2ndTriangle = 5, lengthOf3rdTriangle = 5 và kết quả của areaTriangle
+ Vấn đề: 
. Không có

#### Example 19:
+ Đề bài: Viết chương trình C++ để tìm diện tích và chu vi của một hình tròn.
+ Cách phân tích: 
. Dùng biến double để khai báo:
    double radiusCircle = 5
    double areaCircle = pi * pow(radiusCircle, 2)
    double circumferenceCircle = 2 * pi * radiusCircle
. Dùng cout để in ra các giá trị của radiusCircle = 5 và kết quả của areaCircle với kết quả của circumferenceCircle
+ Vấn đề:
. Không có

#### Example 20:
+ Đề bài: Viết một chương trình C++ để chuyển đổi nhiệt độ từ Celsius (°C) sang Fahrenheit (°F).
+ Cách phân tích:
. Dùng biến double để khai báo:
    temperatureCelsius = 35
    temperatureFahrenheit = (temperatureCelsius  * 9/5) + 32
. Dùng cout để in ra giá trị của temperatureCelsius = 35 và kết quả của temperatureFahrenheit
+ Vấn đề:
. bài toán sử dụng công thức mà người viết chương trình chưa được học nên chưa nắm vững được cách đổi

---------------------------------------------------------------------------------------
### Part: Basic Algorithm
#### Example 1: 
+ Đề bài: Viết một chương trình C++ để tính tổng của hai số nguyên cho trước. Nếu hai số nguyên này bằng nhau thì trả về gấp ba lần tổng của chúng.
+ Cách phân tích: 
. Cho người dùng nhập vào hai số a và b là 2 số nguyên( int)
. Sử dụng if với điều kiện a được so sánh bằng với b thì in ra resultEx1 = (a + b) * 3
. SỬ dụng else với điều kiện còn lại thì in ra resultEx1 = a + b;
. Sử dụng cout để in ra kết quả của result sau khi nhập hai số a và b
+ Vấn đề:
. Chưa thuần thục trong việc gọi biến phù hợp cho người dùng nhập vào 

#### Example 2:
+ Đề bài: Viết chương trình C++ để tìm giá trị tuyệt đối của hiệu giữa n và 51. Nếu n lớn hơn 51 thì trả về gấp ba lần giá trị tuyệt đối đó.
+ Cách phân tích: 
. Cho người dùng nhập vào 1 số nguyên n (int) 
. Dùng biến int để khai báo diff = n - 51
. Dùng if với điều kiện diff < 0 thì in ra resultEx2 = diff * -1
       else với các trường hợp còn lại thì in ra resultEx2 = diff       
. Dùng cout để in kết quả của resultEx2 ra màn hình
+ Vấn đề:
. Không có

#### Example 3:
+ Đề bài: Viết chương trình C++ để kiểm tra hai số nguyên cho trước, và trả về true nếu một trong hai số bằng 30 hoặc tổng của chúng bằng 30.
+ Cách phân tich:
. Cho người dùng nhập vào hai số nguyên a và b
. Dùng bool khai báo resultEx3 có hai giá trị là true=1 và false=0
. Sử dụng if với điều kiện biến a được so sánh bằng 30 hoặc biến b được so sánh bằng 70 hoặc a + b = 30 thì in ra resultEx3 = true
          else với các trường hợp còn lại thì in ra resultEx3 = false
. Dùng cout để in ra màn hình kết quả của resultEx3 với hai số nguyên mà người dùng nhập vào
+ Vấn đề : 
. Chưa thuộc được kiểu dấu hoặc || trong điều kiện

#### Example 4: 
+ Đề bài: Viết chương trình C++ để kiểm tra một số nguyên cho trước và trả về true nếu số đó nằm trong khoảng cách 10 đơn vị so với 100 hoặc 200.
+ Cách phân tích: 
. Cho người dùng nhập vào số nguyên a
. Dùng biến bool để khai báo resultEx4 với hai giá trị là true hoặc false
. Dùng biến int để khai báo lowerBoundOf100 = 90
                            upperBoundOf100 = 110
                            lowerBoundOf200 = 190
                            upperBoundOf200 = 210
. Với điều kiện số nguyên a bé hơn hoặc bằng 90 và lớn hơn hoặc bằng 110 hoặc số nguyên a bé hơn hoặc bằng 190 và bé hơn hoặc bằng 210 thì in ra màn hình:
                        resultEx4 = true
. Với các trường hợp còn thì in ra resultEx4 = false
. Dùng cout để in ra màn hình kết quả của resultEx4 với 3 điều kiện trên

#### Example 5: 
+ Đề bài: Viết chương trình C++ để tạo một chuỗi mà "if" được thêm vào đầu của chuỗi cho trước. Nếu chuỗi đã bắt đầu bằng "if" thì trả về chuỗi không thay đổi.
+ Cách phân tích: 
. Dùng string để khai báo chuỗi kí tự s, chuỗi firstName và resultEx5
. Cho người dùng nhập vào chuỗi kí tự s
. Dùng int để khai báo độ dài của chuỗi length = s.lengt()
. Sử dụng vòng lặp for với điều kiện int i = 0; i <= length; i++ và sử dụng điều kiện lồng bên trong dòng lặp:
Nếu s[i] được so sánh bằng khoảng trống thì dừng vòng lặp và firstName = firstName+s[i] 
. Sử dụng câu lệnh điều kiện If else với điều kiện firstName = "if" thì in ra resultEx5 bằng chuỗi đã nhập vào là s và sử dụng else với các điều kiện còn lại thì in ra resultEx5 bằng if ra trước chuỗi s
+ Vấn đề: Chưa hình dung được cách mà code sẽ hoạt động

#### Example 6:
+ Đề bài: Viết chương trình C++ để xóa ký tự tại một vị trí cho trước trong chuỗi. Vị trí cho trước sẽ nằm trong phạm vi từ 0 đến độ dài chuỗi - 1 (bao gồm cả 0 và độ dài chuỗi - 1).
+ Cách phân tích: 

. Dùng biến string để gọi chuỗi cho người dùng nhập và là s và chuỗi kết quả bài tập 6 là resultEx6
. Dùng biến int để gọi độ dài của chuỗi là một số nguyên length= s.length()
. Sử dụng vòng lập for với các giá trị (int i = 0; i < length; i++) và lồng vào điều kiện if bên trong vòng lặp với điều kiện i được so sánh bằng với n thì tiếp tục xuống dưới resultEx6 += s[i] và in ra i : s[i] : kết quả ví dụ 6 resultEx6
+ Vấn đề:
. Chưa thành thục trong việc sử dụng vòng lặp for với điều kiện if lồng bên trong

#### Example 7:
+ Đề bài: Viết chương trình C++ để hoán đổi ký tự đầu tiên và ký tự cuối cùng trong một chuỗi cho trước, sau đó trả về chuỗi kết quả.
+ Cách phân tích:
. Dùng biến string để gọi chuỗi cho người dùng nhập và là s với lệnh getline(cin, s) và chuỗi kết quả bài tập 7 là resultEx7
. Dùng int để khai báo độ dài của chuỗi length = s.length()
. Với điều kiện length khác bằng 1 thì firstPosition được gán bằng 0 và lastPosition được gán bằng length - 1( lồng vào trong 1 điều kiện if, else if, else với if điều kiện i được so sánh bằng firstPosition thì resultEx7 sẽ cộng bằng s[lastPosition], else if với điều kiện i được so sánh bằng lastPosition thì resultEx7 sẽ cộng bằng s[firstPosition], else với các điều kiện còn lại thì resultEx7 sẽ cộng bằng s[i])
. Với else tất cả điều kiện ở điều kiện if else ngoài thì resultEx7 sẽ bằng chuỗi kí tự s
. Dùng cout để in ra resultEx7
+ Vấn đề:
. Chưa nắm rõ được cách lồng thêm điều kiện if, else if, else vào trong vòng lặp for ở trong 1 điều kiện if else ngoài

#### Example 8:
+ Đề bài: Viết chương trình C++ để tạo ra một chuỗi gồm 4 bản sao của 2 ký tự đầu tiên từ chuỗi cho trước.
Nếu độ dài của chuỗi nhỏ hơn 2 thì trả về chuỗi ban đầu.
+ Cách phân tích: 
. Độ dài của chuỗi kí tự phải lớn hơn hoặc bằng 2 nếu nhỏ hơn thì trả về chuỗi ban đầu
. Xác định hai kí tự đầu tiên của chuỗi kí tự đã cho sẵn
. Sử dụng vòng lặp for loop để tạo ra chuỗi kí tự gồm 4 bản sao của 2 kí tự đầu tiên
+ Vấn đề:
. Kiến thức về vòng lăp for loop: chưa biết vòng lặp dùng để làm gì?
. Khó hình dung cách vòng lặp hoạt động
. Phân tích thiếu đề bài 
+ Giải pháp:
. Đọc lại tutorial của vòng lặp
. Viết ra giấy mô phỏng kết quả của từng vòng lập khi giải bài toán 1

#### Example 9:
+ Đề bài: Viết chương trình C++ để tạo một chuỗi mới, trong đó ký tự cuối của chuỗi gốc được thêm vào đầu và cuối của chuỗi. (Chuỗi đầu vào có độ dài ≥ 1).
. Dùng biến string để gọi chuỗi cho người dùng nhập và là s với lệnh getline(cin, s) và chuỗi kết quả bài tập 9 là resultEx9 và gọi lastCharacter 
.  Dùng int để khai báo độ dài của chuỗi length = s.length()
. Sử dụng if với diều kiện length lớn hơn hoặc bằng 1 thì lastCharacter bằng s[length - 1] và resultEx9 sẽ bằng  lastCharacter cộng chuỗi s cộng  lastCharacter 
. Else với các điều kiện còn lại thì resultEx9 bằng s[1] cộng s[1] cộng s[1]
. Dung cout in ra resultEx9
+ Vấn đề:
. Không có
